#include "main.h"
#include <stdio.h>

/**
 * _isupper - checks for uppercase character
 * @c: character under review
 * Return: 1 if uppercase, otherwise 0
 */
int _isupper(int c)
{
	return ((c >= 65) && (c <= 90));
}
