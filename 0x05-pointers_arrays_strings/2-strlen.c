#include "main.h"
#include <stdio.h>

/**
 * _strlen - return length of string
 * @s: string pointer
 * Return: length of string
 */
int _strlen(char *s)
{
	int counter = 0;

	while (*s != '\0')
	{
		count++;
		s++;
	}
	return counter;
}
