#include <stdio.h>
#include "main.h"

/**
 * _islower - returns 1 if c is lower
 *
 *  @c - character
 * Return: 1 if lower case otherwise 0
 */
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}
